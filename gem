<?php
use Gem\System\Run;
include "vendor/autoload.php";

 $system = new Run();
 $args = isset($argv) ? $argv:[];
 $argc = isset($argc) ? $argc:0;

use Gem\Components\Patterns\Singleton;

$console = Singleton::make('Gem\Components\Console\Console', [$argv, $argc]);
$console->run();